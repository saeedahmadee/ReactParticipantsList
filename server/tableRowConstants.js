const tableRowFieldTypes = ["_id", "name", "email", "address", "phoneNumber"];

const filterabletableRowFieldTypes = ["name", "email", "address", "phoneNumber"];

const orderFieldTypes = ["ascending", "descending"];

const defaultLimit = 10;
const defaultOffset = 0;

module.exports = {
	tableRowFieldTypes,
	filterabletableRowFieldTypes,
	orderFieldTypes,
	defaultLimit,
	defaultOffset
};
